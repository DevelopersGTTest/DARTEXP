import 'package:flutter/material.dart';

class SubmitVal extends StatelessWidget {
 @override
  Widget build(BuildContext context) {
    return RaisedButton(
      child: Text("Send"),
      onPressed: (){},
    );
 }
}